.center-container
  .absolute-center.text-center
    %h1.headline EXPERTS
    - if user_signed_in?
      .action-button Thanks for signing up!
    - if current_user && !current_user.confirmed?
    - else
      = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
        = devise_error_messages!
        .hidden-input-field
          = f.text_field :first_name, :autofocus => true, placeholder: "First Name"
        .hidden-input-field
          = f.text_field :last_name, :autofocus => true, placeholder: "Last Name"
        .hidden-input-field
          = f.text_field :username, :autofocus => true, placeholder: "Username"
        .hidden-input-field
          = f.text_field :email, :autofocus => true, placeholder: "Email"
        .hidden-input-field
          = f.text_field :mobile_number, :autofocus => true, placeholder: "Mobile Number"
        .hidden-input-field
          = f.text_field :expertise, :autofocus => true, placeholder: "Expertise"
        .hidden-input-field
          = f.password_field :password, placeholder: "Password"
        .action-button.join-us Join us
        .action-button.submit.hide= f.submit "Submit"


