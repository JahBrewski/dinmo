%h1.center Reply
.conversation
  = form_for @message do |f|
    = f.text_area :body, :rows => "5"
    = f.hidden_field :conversation_id, :value => @conversation.id
    = f.hidden_field :sender_id, :value => @conversation.expert.id
    .submit
      = f.submit
