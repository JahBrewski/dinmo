- if @conversations.any?
  - @conversations.each do |conversation|
    %ul
    - conversation.messages.each do |message|
      %li= message.body
      %li= link_to "reply", new_message_path(:id => message.conversation)
- else
  No messages
